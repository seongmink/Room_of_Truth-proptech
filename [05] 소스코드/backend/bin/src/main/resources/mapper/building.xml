<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="building">

	<insert id="createBuilding" parameterType="building" useGeneratedKeys="true" keyProperty="num" keyColumn="building.num">
		insert into
		building(address, dong, ho, latitude, longitude, supply, exclusive,
		details, cost, license, image, createdAt)
		values(#{address}, #{dong}, #{ho}, #{latitude},
		#{longitude}, #{supply}, #{exclusive}, #{details}, #{cost}, #{license}, #{image}, now())
	</insert>

	<select id="getAllBuilding" resultType="building">
		select distinct address,
		dong, ho, latitude, longitude
		from building
	</select>

	<select id="getRecentNum" parameterType="building"
		resultType="java.lang.Long">
		select num
		from building
		where num in (select num from
		building
		where address=#{address} and dong=#{dong} and ho=#{ho})
		order
		by num desc limit 1
	</select>

	<select id="getBuildingNum" parameterType="building"
		resultType="building">
		select * from building where address like #{address} and
		dong like #{dong}and ho like #{ho};
	</select>
	
	<select id="getBuildingByNum" parameterType="java.lang.Long" resultType="building">
		select * from building where num=#{num}
	</select>
	
	<select id="getAgentContribution" parameterType="java.lang.String" resultType="building">
		select * from building where license=#{license}
	</select>
	
    <select id="getDetailList" parameterType="building"
        resultType="building">
        
        select distinct address, dong, ho ,longitude, latitude from (
        select distinct address, dong, ho ,longitude, latitude from building
        where latitude=#{latitude} and longitude=#{longitude}
        order by dong asc, ho asc
    </select>
    
    <select id="getBImage" parameterType="building"
        resultType="java.lang.String">
        select image from building
        where address=#{address} and dong=#{dong} and ho=#{ho}
        order by num desc limit 1
    </select>
    
</mapper>

